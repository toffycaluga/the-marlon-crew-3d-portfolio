---
---

<div class="three-wrapper">
  <canvas id="three-canvas"></canvas>

  <div class="overlay-ui">
    <button data-view="center">Vista general</button>
    <button data-view="trapecio">Trapecio</button>
    <button data-view="publico">Público</button>
    <button data-view="entrada">Entrada</button>
  </div>
</div>

<style>
  .three-wrapper {
    width: 100vw;
    height: 100vh;
    margin: 0;
    padding: 0;
    overflow: hidden;
    position: relative;
  }

  #three-canvas {
    width: 100%;
    height: 100%;
    display: block;
  }

  .overlay-ui {
    position: absolute;
    top: 1rem;
    left: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    z-index: 10;
  }

  .overlay-ui button {
    background: rgba(0, 0, 0, 0.7);
    border: 1px solid #ffffff33;
    color: #f5f5f5;
    padding: 0.35rem 0.75rem;
    font-size: 0.85rem;
    border-radius: 999px;
    cursor: pointer;
  }

  .overlay-ui button:hover {
    background: rgba(255, 255, 255, 0.1);
  }
</style>

<script>
  import { initThreeScene } from '../three/initScene.js'; // o '@/three/...' si tienes alias

  if (typeof window !== 'undefined') {
    window.addEventListener('DOMContentLoaded', () => {
      const canvas = document.getElementById('three-canvas');
      if (canvas) {
        initThreeScene(canvas);
      }

      const buttons = document.querySelectorAll('.overlay-ui [data-view]');
      buttons.forEach((btn) => {
        btn.addEventListener('click', () => {
          const view = btn.getAttribute('data-view');
          if (window.setThreeView && view) {
            window.setThreeView(view);
          }
        });
      });
    });
  }
</script>
